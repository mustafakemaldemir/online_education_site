@model List<online_education_site.EntityFramework.Models.Lesson>

<!--<h1>Add Document Page</h1>-->
<h3 class="fw-bold mb-2">Select Class For Upload Document</h3>

<div class="row">
    <div class="col-2 mx-2">
        
        <ul class="list-group">
            @foreach (var lesson in Model)
            {
                <li class="list-group-item">
                    <a href="@Url.Action("AddDocumentLesson", "Lessons", new {id = lesson.LessonId})">
                        @lesson.LessonClassId @lesson.LessonName
                    </a>
                </li>
            }
        </ul>
    </div>
</div>